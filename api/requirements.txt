# pandemic-data-platform — FastAPI service dependencies
# Python 3.11+

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.*
uvicorn[standard]==0.29.*      # ASGI server with uvloop + httptools
python-multipart==0.0.*        # required for form data support

# ── Database (reuses ETL models) ─────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.*
asyncpg==0.29.*
alembic==1.13.*

# ── HTTP client (R microservice calls) ───────────────────────────────────────
httpx==0.27.*
tenacity==8.3.*

# ── Validation / serialisation ────────────────────────────────────────────────
pydantic==2.7.*
pydantic-settings==2.2.*
python-dotenv==1.0.*

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==6.1.*
structlog==24.1.*

# ── ETL models (shared SQLAlchemy ORM) ───────────────────────────────────────
# Installed from the local package in the same repo (see Dockerfile COPY ../etl)
# No separate entry here — added to PYTHONPATH via Dockerfile
