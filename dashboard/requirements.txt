# pandemic-data-platform — Streamlit dashboard dependencies
# Python 3.11+

streamlit==1.35.*
plotly==5.22.*           # interactive charts (area, bar, scatter, choropleth)
pandas==2.2.*
httpx==0.27.*            # async API client
python-dotenv==1.0.*
