# Test dependencies (added on top of api/requirements.txt + etl/requirements.txt)
pytest==8.2.*
pytest-asyncio==0.23.*
pytest-cov==5.0.*
httpx==0.27.*
aiosqlite==0.20.*        # async SQLite driver for in-memory test DB
